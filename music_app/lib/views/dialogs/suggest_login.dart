import 'package:flutter/material.dart';

class SuggestLogin extends StatelessWidget {
  const SuggestLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Login to use all futures');
  }
}
